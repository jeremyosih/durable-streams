---
"@durable-streams/server": patch
"@durable-streams/server-effect-postgres": patch
---

Add an Effect v4 durable streams server backed by Postgres, and allow the shared Node server to run against injected async storage implementations.
